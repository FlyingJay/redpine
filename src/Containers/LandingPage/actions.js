import { push } from 'react-router-redux'

import { paths } from 'globals'
import { errorMessage } from 'Components/errors'

import constants from './constants'


const actions = {
/*
  loadData: () => {
    return (dispatch, getState, api) => {
      dispatch(actions.landingpageLoading()) 
      api.ready.then(() => {
        api.PICK_SOMETHING.query().then((res) => {
          console.log(res)
          dispatch(actions.landingpageLoaded())
        })
      })
    }
  },
 
  landingpageLoading: () => { 
    return { 
      type: constants.LANDINGPAGE_LOADING
    }
  },

  landingpageLoaded: () => { 
    return { 
      type: constants.LANDINGPAGE_LOADED
    }
  }
*/
}

export default actions

